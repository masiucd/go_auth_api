import * as logger from "../logger"

describe("singleton , logger", () => {
  const l = logger.logger

  test("should have expected structure", () => {
    //
  })
  test("should only be one instance of the logger class", () => {
    //
  })
})
